# Copy to .env and edit (then chmod 600 .env):
#   cp .env.example .env
#   chmod 600 .env

# Optional: enable bearer-token auth on bridge endpoints. Generate with:
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
# Leave empty for no auth (LAN-only deployment is OK to leave open).
BRIDGE_TOKEN=
